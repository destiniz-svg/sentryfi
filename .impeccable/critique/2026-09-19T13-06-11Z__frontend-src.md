---
target: whole product
total_score: 14
max_score: 40
na_heuristics: 
p0_count: 3
p1_count: 3
target_identity: "file:C:\\Users\\THINAN\\ledgeros\\frontend\\src"
timestamp: 2026-09-19T13-06-11Z
slug: frontend-src
---
**Method: dual-agent.** Assessment A (design review, source + artboards, no detector) and Assessment B (detector, contrast arithmetic, bundle measurement, ESLint) ran as two isolated parallel subagents. Not degraded.

**Coverage:** all 14 routes in `frontend/src`, the token layer, UI primitives, both artboard sets, and the built `frontend/dist`. No browser — none installed, none exposed. Every figure computed from source or measured from the build.

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 1 | Every `toast()` throws. `UIContext.jsx:44` references undefined `t`; ESLint `no-undef` confirms. 8 dead call sites. Zero `aria-busy`. |
| 2 | Match System / Real World | 1 | `formatMoney` defaults USD (`lib/utils.js:23`), 20 call sites. PayPal/Check methods. Software/Hosting/Meals categories. |
| 3 | User Control and Freedom | 2 | No undo anywhere though DESIGN.md specifies 10s. `InvoiceEditor.jsx:185` discards typed invoice, no dirty check. |
| 4 | Consistency and Standards | 1 | Two visual worlds: teal auth vs yellow app. Three confirmation patterns. ~11 undocumented type steps. |
| 5 | Error Prevention | 2 | 7 destructive actions guarded, but mark-paid bypassable via `<select>` at `InvoiceEditor.jsx:246`. Dates built in UTC. |
| 6 | Recognition Rather Than Recall | 1 | 17 inputs with no label incl. all 4 password fields. 13 unnamed icon buttons. Sidebar labels hover-only, no focus-within. |
| 7 | Flexibility and Efficiency | 1 | One shortcut in the app. Zero bulk actions. `ui/Checkbox.jsx` imported by nothing. |
| 8 | Aesthetic and Minimalist Design | 2 | Five decorative layers behind login. `Reports.jsx` nine focal regions. |
| 9 | Error Recovery | 2 | Plain language, but no `role="alert"` on any of 9 banners. Zero `sr-only`. |
| 10 | Help and Documentation | 1 | No help surface. `Login.jsx:84` "Forgot?" has no `onClick`. |
| **Total** | | **14/40** | **Poor — major UX overhaul required** |

## Design Specificity Verdict

Two products ship under one name; the seam is the login screen. Above it `Landing.jsx` could not be reused by anyone else. Below it an unrelated SaaS could use this unchanged, because one already does: `AuthShell.jsx:101` ships "AI Invoice Manager"; `Register.jsx:44` promises "Free to start. No credit card required." for a product with no billing. `routes.jsx` has no Bills, Cash boxes, Bank, Projects or Tax centre; no camera route at all.

**Deterministic scan:** 59 findings, 4 rules. `frontend/src` 56; `App.dc.html` 0 (partly suppressed via 2 wildcard ignoreValues); `Web.dc.html` 3.

- 5 `warning` findings (the only non-zero exit driver) are FALSE POSITIVES: @react-pdf base-14 font names.
- 42 font-size findings are ~11 distinct steps counted 42 times.
- `index.css:115` scrollbar radius: artifact.
- Colour findings REAL: `#0c3b36`, `#071a18`, two emerald/teal glows on the auth screen.

**Visual overlays:** none. No browser automation installed, no browser tool exposed. No overlay rendered.

## Overall Impression

The backend got rigorous and the front end did not move. The biggest opportunity is that the front end now actively contradicts the back end: `Invoices.jsx:53` says "This cannot be undone" in a product whose ledger cannot delete anything and records corrections as reversing entries. Every list shows a trash can for records the app has no database permission to destroy.

## What's Working

1. `Dashboard.jsx` refuses to invent the number it cannot compute ("In bank and cash — Not connected yet").
2. `ErrorPage.jsx` answers the real fear: "Nothing is wrong with your books."
3. `MobileNav.jsx` — 1 of 9 overlays that is complete; `aria-modal` appears exactly once in the codebase, here.

## Priority Issues

### [P0] Entire feedback layer throws on every call

`context/UIContext.jsx:44` undefined `t`; ESLint `no-undef`. All 8 toast sites dead, including the success path of marking an invoice paid. `ToastViewport` is correctly built with `role="status" aria-live="polite"` and stays permanently empty. Fix: `variant === "error"`. Command: `/impeccable harden`

### [P0] Two sections unreachable by keyboard

`Clients.jsx:73`, `Items.jsx:57` — `<div onClick>` with no role/tabIndex/key handler. `Invoices.jsx` and `Expenses.jsx` were fixed; these were missed. No fallback path. Command: `/impeccable audit`

### [P0] Dark mode makes its own theme picker invisible

`index.css:56` sets `--accent-strong: #ffffff` in dark, and `Settings.jsx:264,276` render `bg-[var(--accent-strong)] text-white` = 1.00:1. Also still present: 1.67:1 yellow-on-white invoice title in the generated PDF (`InvoiceDocument.jsx:27`); 2.64:1 `#9AA0A8` twice in `Landing.jsx:318,475`. Caveat: derived arithmetically, not rendered. Command: `/impeccable audit`

### [P1] Four modals cannot be escaped

`ExpenseModal`, `ItemModal`, `RecordPaymentModal`, `ClientFormModal`: none of Escape/trap/restore/role/aria-modal/scroll-lock. `CommandPalette` partial (Escape bound to input only). `MobileNav.jsx` has the correct implementation to copy. Command: `/impeccable harden`

### [P1] Money shown in US dollars; dates built in UTC

`lib/utils.js:23` defaults USD across 20 call sites. `InvoiceEditor.jsx:36`, `Expenses.jsx:183`, `Payments.jsx:102` use `toISOString().slice(0,10)` — between midnight and 5am Maldives time every default date is yesterday, filing transactions into the wrong GST period. Command: `/impeccable clarify`

### [P1] 2.35 MB in one uncompressed chunk

Verified: `dist/assets/index-Coymkaan.js` = 2,349,782 bytes, one chunk, zero code splitting. `backend/src/server.js:63` serves via `express.static` with no compression middleware installed. `recharts` and `@react-pdf/renderer` load on `/login`. Caveat: edge compression unverified. Command: `/impeccable optimize`

## Persona Red Flags

**Alex (power user):** one shortcut total; no save shortcut in the editor; zero bulk actions (30 bills = 30 round trips); Escape broken on his three most-used modals.

**Sam (accessibility):** 17 unlabelled inputs incl. all 4 password fields; 13 unnamed icon buttons; no `role="alert"` on 9 error banners; no skip link; zero `sr-only` and zero `aria-busy`; every page heading level 1 is "Hello, Thinan."; `Input.jsx:22` focus ring is yellow at 20% alpha on white.

**Casey (one-handed, on site):** 35 touch targets under 44px (9 at 28px, Edit beside Delete, forced visible on touch); primary action in header, hamburger in thumb zone; no URL state; no draft persistence; no service worker or manifest while `Landing.jsx:71` advertises offline.

**Procurement officer (project-specific):** the role does not exist in the front end. `AuthContext` carries name and email only; no role anywhere in `src`. Sees every client, invoice and balance. The ledger enforces company isolation in the database; the front end does not know companies exist.

## Minor Observations

`ThemeContext.jsx:5` still uses the purchased `"arr-theme"` key. Brand component named `AILogo`. `Reports.jsx:196` ships an emoji. No TIN or GST field in Settings. `Expenses.jsx:72` missing `capture="environment"`. `StatCard.jsx:75` orphaned closing div. ESLint: 10 set-state-in-effect, 7 unused vars.

**Clean result:** `react-hooks/rules-of-hooks` fired 0 times. The React #310 class of bug is genuinely gone, confirmed by ESLint rather than by eye.

## Questions to Consider

1. The ledger is append-only and the app role has no delete permission. Why does every list still show a trash can, and why does `Invoices.jsx:53` say "This cannot be undone"? What if the only destructive verb were "Reverse, with a reason"?
2. The best interaction in the app exists exactly once (`InvoiceDetail.jsx:137`). What if a control that moves money could not be written without naming the money it moves?
3. Every screen behind `/login` is role-free while the safety model is per-role and per-company. Is step 2 really "get bills in", or is it "the shell knows who you are and which company you are in"?
